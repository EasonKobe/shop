'use strict';
define(function (require){
	//是否开启自动获取菜单信息
	//Tansun.cache.autoGetMenu=true;
	/*Tansun.app.run(function($rootScope){
		$rootScope.selector = {
			//客户选择器 回调事件自己写$rootScope.selector.cst.callback=function(item) {}
			cst : function() {
				return angular.copy({
					header: ['客户名称', '社会信用代码', '客户类型'],
		            body: ['cstNm', 'orgCode', "cstTpShow "],
		            text: 'cstNm',
		            resource : 'customerSelectorList.query' ,
		        }) ;
			},
		} ;
		
	});*/
});
	